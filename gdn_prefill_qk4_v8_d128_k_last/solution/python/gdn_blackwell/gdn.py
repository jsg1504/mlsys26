def chunk_gated_delta_rule(*, q, k, v, g, beta, initial_state, cu_seqlens, scale):
    raise NotImplementedError(
        "gdn_blackwell.chunk_gated_delta_rule is a Python stub until the Blackwell runtime is vendored."
    )
